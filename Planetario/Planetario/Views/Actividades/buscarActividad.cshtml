
@{
    ViewBag.Title = "Actividades";
}

<!DOCTYPE html>
<html>
<head>
    <title>Actividades</title>
    <meta charset="utf-8" />
    <link rel="stylesheet" type="text/css" href="~/Utils/css/main.css">
</head>
<body>
    <span class="titulo">
        Actividades
    </span>

    <form method="post" class="navbar-form navbar-right" action="@Url.Action("listadoDeActividades", "Actividades")">
        <button type="submit" class="btn btn-secondary"> <span class="glyphicon glyphicon-home" aria-hidden="true"></span> Volver</button>
    </form>

    <table class="table table-hover well">
        <tr>
            <th>Actividad</th>
            <th>Categoria</th>
            <th>Día que se imparte</th>
            <th>Detalles</th>
        </tr>
        @foreach (Planetario.Models.ActividadModel actividad in ViewBag.actividadesUnicas)
        {
            <tr>
                <td>@actividad.NombreActividad</td>
                <td>@actividad.Categoria</td>
                <td>@actividad.DiaSemana</td>
                <td>@Html.ActionLink("ver más", "verActividad", "Actividades", new { nombre = @actividad.NombreActividad.ToString() }, null)</td>
            </tr>
        }
    </table>

</body>
</html>